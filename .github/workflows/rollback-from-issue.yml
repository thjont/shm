name: Rollback main from issue

on:
    issues:
        types: [labeled]

jobs:
    rollback:
        if: github.event.label.name == 'publish' && contains(github.event.issue.labels.*.name, 'rollback')
        runs-on: ubuntu-latest
        permissions:
            contents: write
            issues: write
            pull-requests: write
        steps:
            - name: Check permission
              env:
                  GH_TOKEN: ${{ github.token }}
                  SENDER: ${{ github.event.sender.login }}
                  ISSUE_NUMBER: ${{ github.event.issue.number }}
                  REPO: ${{ github.repository }}
              run: |
                  PERMISSION=$(gh api "repos/${REPO}/collaborators/${SENDER}/permission" --jq '.permission' 2>/dev/null || echo "none")
                  if [[ "$PERMISSION" != "admin" && "$PERMISSION" != "write" ]]; then
                    gh issue comment "$ISSUE_NUMBER" --body "@${SENDER} Only maintainers with write access can trigger a rollback."
                    gh issue close "$ISSUE_NUMBER"
                    exit 1
                  fi

            - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1
              with:
                  ref: main

            - name: Revert and create PR
              env:
                  GH_TOKEN: ${{ github.token }}
                  ISSUE_NUMBER: ${{ github.event.issue.number }}
                  ISSUE_BODY: ${{ github.event.issue.body }}
              run: |
                  python3 - <<'PYEOF'
                  import os, re, subprocess

                  body = os.environ['ISSUE_BODY']
                  issue_number = os.environ['ISSUE_NUMBER']

                  def extract(label):
                      m = re.search(rf'### {re.escape(label)}\n\n(.*?)(?=\n### |\Z)', body, re.DOTALL)
                      if not m:
                          return ''
                      v = m.group(1).strip()
                      return '' if v == '_No response_' else v

                  reason = extract('Reason') or 'No reason provided.'

                  commit_sha = subprocess.check_output(['git', 'rev-parse', 'HEAD']).decode().strip()
                  commit_msg = subprocess.check_output(['git', 'log', '-1', '--pretty=%s']).decode().strip()
                  short_sha = commit_sha[:7]
                  branch = f'rollback/issue-{issue_number}'

                  subprocess.run(['git', 'config', 'user.name', 'github-actions[bot]'], check=True)
                  subprocess.run(['git', 'config', 'user.email', 'github-actions[bot]@users.noreply.github.com'], check=True)
                  subprocess.run(['git', 'checkout', '-b', branch], check=True)
                  subprocess.run(['git', 'revert', 'HEAD', '--no-edit'], check=True)
                  subprocess.run(['git', 'push', 'origin', branch], check=True)

                  pr_body = f'Reverts {short_sha} ("{commit_msg}")\n\n**Reason:** {reason}\n\nCloses #{issue_number}'

                  subprocess.run([
                      'gh', 'pr', 'create',
                      '--title', f'Rollback: {commit_msg}',
                      '--body', pr_body,
                      '--base', 'main',
                      '--head', branch,
                  ], check=True)
                  PYEOF
