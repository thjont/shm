# Cloudflare Pages configuration for stage-shiny-hoppy-meeple.
# Copied over wrangler.toml by the stage deploy; see DEVELOPMENT.md.

name = "stage-shiny-hoppy-meeple"
pages_build_output_dir = "public"
compatibility_date = "2026-07-01"

# Create with: wrangler kv namespace create SCANS
# then paste the returned id below.
[[kv_namespaces]]
binding = "SCANS"
id = "3d63a5f25e204081bcb066358e8d9109"
